ifeq "$(shell uname -s)" "Darwin"
INCLUDE_PATH := -I../../../ -I/usr/X11/include
LIBRARY_PATH := -L../../Lib -L/usr/X11/lib
LINK_LIBRARY := -lKVS.osmesa -lOSMesa
else
ifeq "$(shell uname -s)" "Linux"
INCLUDE_PATH := -I../../../
LIBRARY_PATH := -L../../Lib -L/usr/lib/x86_64-linux-gnu
LINK_LIBRARY := -lKVS.osmesa -lOSMesa
endif
endif
