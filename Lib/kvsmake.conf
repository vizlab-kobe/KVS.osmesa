ifeq "$(shell uname -s)" "Darwin"
INCLUDE_PATH := -I/usr/X11/include
LIBRARY_PATH := -L/usr/X11/lib
LINK_LIBRARY := -lOSMesa
else
ifeq "$(shell uname -s)" "Linux"
INCLUDE_PATH := 
LIBRARY_PATH := -L/usr/lib/x86_64-linux-gnu
LINK_LIBRARY := -lOSMesa
endif
endif
